# Copy to .env and change the password before anyone else can reach this.
STAFF_PASSWORD=bluefig
SESSION_SECRET=change-me-to-something-random
# Anything SQLAlchemy speaks. Postgres needs its driver added, e.g.
# postgresql+psycopg://nextable:secret@localhost/nextable
DATABASE_URL=sqlite:///./nextable.db
